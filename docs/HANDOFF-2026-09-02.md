# Handoff

## State
Plates + Chart Room redo, 2026-09-02: everything merged and gated on
`claude/plates-integration-2026-09-02` at cbccae59b (origin) — August plate
branches, Chart Room postcard, two-plate panorama (A "The Bay and Ilios", B
"The Ships on the Aegean Shore"), enriched geographic sheet, schematic sheet
rebuilt on real ground east-up with Pope's numbered key, apparatus corrected
against the Greek, American-spelling sweep + house-style test. Gate: shared
1235, app 27, pytest 765, build:public 4,704 pages, preflight ok, 0 broken.
Rulings 1–8 in `docs/TROY-MAPS-TODO.md` § "Rulings 2026-09-02". Nothing on
main, nothing deployed.

## Next
1. Ship glyphs (John: "too big, ugly, obtrusive"): finish the two comps parked
   UNCOMMITTED in `.claude/worktrees/plates-shipcomp` (option 1 thin outline
   hull, six a rank, `style:"light"`; option 2 Pope's hatched band, hulls only
   in the inset); send John both camp crops; he picks.
2. John's calls: Trojan bivouac (fan vs literal ground), plates A/B verdict,
   Blindow–Hübner–Jansen 2014 OA download for the Troy VI ditch, a live
   browser look at the postcard/tooltip (subagent sandbox blocked localhost).
3. Stage 6: Sol adversarial review of the whole integration diff, then PR to
   main (carries the July-28 cross-epic search/phrases too). Deploy is John's.

## Context
Grok forwarders end their turn while the run continues — poll `ps`/nudge; Grok
implements from tight briefs, Sonnet looks and fixes, John judges the image.
`parsePlate` does not check a featureKey placeId is anchored (Python does).
Render plates with `scripts/render-plates.mjs` (Playwright path gotcha in
CLAUDE.md). Memory: `plates-chart-room-state.md` has the full state.
